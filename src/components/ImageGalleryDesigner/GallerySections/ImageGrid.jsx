import React from 'react';
import './ImageGrid.scss';

const ImageGrid = ({ section, onSectionUpdate }) => {
  return (
    <div className="image-grid-section">
      {/* Add image grid implementation here */}
    </div>
  );
};

export default ImageGrid;
